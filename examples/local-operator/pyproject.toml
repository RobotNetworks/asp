[project]
name = "asp-local-operator"
version = "0.1.0"
description = "Reference local-network operator for ASP. In-memory, single-process."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "pydantic>=2.7",
    "websockets>=13.0",
]

[tool.uv]
package = false
